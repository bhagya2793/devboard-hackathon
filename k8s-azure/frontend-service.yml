apiVersion: v1
kind: Service
metadata:
  name: devboard-frontend-service
  namespace: devboard
spec:
  type: LoadBalancer
  selector:
    app: devboard-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4173
